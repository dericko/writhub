<div class="row">
  <div class="twelve columns">
    <p>Hi {{username}}! Welcome to the dashboard.</p>
    <p>You are logged in. Click <a href="/logout">here</a> to log out!</p>

    {{#each filesInHome}}
    <div class="row mt centered">
      <a href={{html_url}} >{{name}}</a>
    </div>
    {{/each}}

  </div>
</div>